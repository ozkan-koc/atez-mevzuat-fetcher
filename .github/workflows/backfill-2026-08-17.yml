name: Backfill 2026-08-17

on:
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  fetch:
    if: false
    runs-on: ubuntu-latest
    steps:
      - run: echo 'backfill completed; workflow disabled pending deletion'
